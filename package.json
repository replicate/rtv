{
  "name": "rtv",
  "version": "1.0.0",
  "description": "Writer generates tiles on Replicate. They are saved in the directory `broadcast-queue` as two files:",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "make-promises-safe": "^5.1.0",
    "replicate": "^0.3.0"
  }
}
